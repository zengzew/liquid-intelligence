# 2026-07-29

## Liquid Intelligence — V2.3 Iteration 1(Ocean 终章重建)

- 完成 Prior 案例研究 + 15 张基线截图 Review,确定最阻碍 award-level 的问题:Ocean 终章(Night 是失焦灰斑无地平线,Morning 白上叠白,远裁剪面 140 会切出硬边)。
- 重写 `src/journey/worlds/OceanWorld.tsx`:专用 GLSL 海面(距离渐变体色 + 手工雾在远裁剪面前完全融入背景)+ 世界空间月光/日光路径(各向异性闪光拉伸成水面光带)+ 正弦平滑地平线辉光。仅改这一个文件,其余未提交工作全部保留。
- 验证:tsc clean、build clean、全部场景 runtimeErrors=0;journey-after 全套截图确认 Origin→Tributaries 与基线像素一致。
- 证据与完整 Review:`design-reviews/v2-3-iteration-1/review.md`。
- 下一轮优先级:① Morning 主题水体全局洗白(需 Morning 专属水体质感分级)② 移动端 Ocean 联系方式与章节索引导航重叠 ③ Night 纯黑虚空可加远处微光/星野增加空间深度。

## Liquid Intelligence — V2.3 Iteration 2(过渡/鼠标动态/影调)

- 用户反馈 4 问题:过渡突兀、缺动态与鼠标响应、离 award-level 远、Night 太黑 Morning 太白;只做桌面端。
- 过渡:Ocean 改为潮汐推进式出现(uAdvance 0.75→0.95 按离镜头距离展开,辉光先行),河尾溶解延迟到 0.84→0.96 且保留 22%——河水可见地流入海中。
- 鼠标动态:CPU 每帧把指针射线投到河面(144 个曲线采样转 across/longitudinal),顶点涟漪环 + 片元聚光;海面同样有环;状态暴露在 `window.__LIQUID_POINTER_WATER__` 便于自动化验证。idle 浮动增强(振幅 +20%,加整体起伏)。
- 影调:Night 背景 #05090b + 半球光 0.3;Morning 背景 #eae4d7、水体加深(#5f7980/opacity 0.76/transmission 0.3)、反射环境 #8fa0a0、辉光 0.3;新增 SkyBackdrop 相机跟随渐变穹顶(抖动防色带)给虚空空间深度。
- 验证:tsc/build clean,全旅程 runtimeErrors=0;证据 design-reviews/v2-3-iteration-2/。
- 下一轮候选:① Origin/Source 段无专属 3D 时刻(OriginWorld/StreamWorld 是死代码)② Morning 海近景仍偏淡 ③ 涟漪可进一步折射闪光路径 ④ 移动端(本轮明确不做)。

## 工具链备忘

- 浏览器截图:playwright-core 已装在 `~/.workbuddy/binaries/node/workspace`,可执行文件用 `~/Library/Caches/ms-playwright/chromium-1223/.../Google Chrome for Testing`,脚本 `capture-liquid.cjs`(全旅程)/ `capture-ocean.cjs`(海洋段)。App 支持 `?progress=0..1&theme=night|morning` URL 参数定格场景,截图前等 3.2s 让 shader 编译稳定。
